{
  "name": "handong-archive",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "bun test",
    "lint": "oxlint src tests",
    "deploy": "wrangler pages deploy dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "oxlint": "^0.9.0",
    "@types/bun": "latest"
  },
  "dependencies": {
    "@chenglou/pretext": "latest",
    "qrcode-generator": "^1.4.4"
  }
}
